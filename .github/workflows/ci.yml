name: CI

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build:
    name: BananoStand CI
    runs-on: ubuntu-latest
    env:
      GITHUB_RUN_ID: ${{ github.run_id }}
    steps:
      - uses: actions/checkout@master

      - name: Unit test
        shell: bash
        run: yarn && yarn test --no-watch

      - name: Prettier Check
        shell: bash
        run: yarn && yarn prettier:check

      - name: Lint Check
        shell: bash
        run: yarn && yarn lint

      - name: Build
        shell: bash
        run: yarn && yarn build

      - name: Cypress Tests
        shell: bash
        run: yarn && yarn cytest

